# Example Queries

Try these queries to see what the agent can do:

## Exploration
- "What does this project do?"
- "List all files in the agent directory."
- "Show me the content of config.py."
- "Explain the logic in agent/indexer.py."

## Search
- "Find the function that handles file reading."
- "Search for where the Gemini model is initialized."

## Modification (Safety checks enabled)
- "Add a comment to the run_agent function in core.py explaining what it does."
- "Create a new file named `tests/test_new.py` with a simple test case."

## Execution
- "Run the tests using python -m unittest."
- "Check the python version."
